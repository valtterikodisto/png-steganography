/**
 * How large portion of 8-bit RGB blue's bits can be
 * used to hide data. Valid numbers are 0b11, 0b1111
 * and 0b11111111
 * @constant
 * @type {number}
 */
const MAX_DATABITS = 0b11

module.exports = {
  MAX_DATABITS
}
